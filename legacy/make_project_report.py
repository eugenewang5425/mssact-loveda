# -*- coding: utf-8 -*-
"""生成项目报告 (v2): 按当前项目状态更新, 去除"完整实验报告"字样
markdown -> HTML -> Chrome headless PDF
"""
import os, base64
import paths  # 集中路径配置 (环境变量/.env)
import markdown

BASE = paths.REPO
FIG = f"{BASE}/figures"

def img_b64(path):
    with open(path, "rb") as f:
        return "data:image/png;base64," + base64.b64encode(f.read()).decode()

def has(p): return os.path.exists(f"{FIG}/{p}")

MD = r"""
# MSSACT-Net 遥感土地覆盖分割
## 项目报告

**数据集**: LoveDA 公开基准（0.3m 航空正射, 7 类土地覆盖, 官方人工标注）
**模型**: MSSACT-Net（多尺度自注意力 ConvTransformer, light 6.10M）
**报告日期**: 2026-09-10

> 本文档为个人项目材料，记录基于毕业设计方向的完整实验工作，供指导教师参考。

---

## 摘要

本项目基于公开遥感基准 **LoveDA**（0.3m 航空正射, 7 类土地覆盖, 官方人工逐像素
标注），对自研 **MSSACT-Net**（多尺度自注意力 ConvTransformer, 6.1M 参数）
开展系统性实验，覆盖数据量效应、后训练、推理增强、对比实验、消融实验与
公平性验证。主要结果：

**1. 数据量是精度主导因素。** 训练数据由 957 张增至 2,257 张（2.4×），
干净测试集 Kappa 由 **0.3921 提升至 0.6114（+0.219）**，OA 由 0.5255 提升至
0.6862。少数类获益最大：道路 F1 **+0.390**、水域 **+0.385**、裸地 **+0.324**、
建筑 **+0.255**，证明早期精度偏低的主因是训练样本不足，而非模型能力缺陷。

**2. 后训练与推理增强提供边际增益。** 在已收敛模型上继续低学习率训练
（lr=5e-5）获得 **+0.012 Kappa**；8× 几何变换 TTA 再获得 **+0.005**。
四阶段递进后最终达到 **Kappa 0.6278 / OA 0.7009 / mF1 0.7089 / mIoU 0.5583**
（192 张干净测试集）。

**3. 模块贡献需按收敛协议重评。** 在统一 120 轮收敛协议下，仅 **ECSAM
注意力为正贡献**（−0.028），Adapter/Transformer/FPN 为负（+0.055 ~ +0.076）；
而 **EMR 的「训练坍缩」系早停误杀**——延长至 120 轮后恢复至 0.4055。这说明
模块价值与训练预算强相关，单预算消融会得出误导性结论。

**4. 训练策略对稳定性至关重要。** 策略探索表明 **SGDR 热重启最优**
（Kappa 0.4183）；**30.5M 原版架构在调优策略下同样可收敛**（0.4186），
证明此前的训练不稳定属于策略问题而非架构缺陷。

**5. 参数效率优势显著。** 在基线对比中，MSSACT-Net 以 **6.1M 参数**取得
最高 Kappa（0.4065），优于 DeepLabV3+（39.6M, 0.3806）、FCN（23.7M, 0.2837）
等模型，占据精度-参数量帕累托前沿。

**6. 标签质量已成为模型能力评价的瓶颈（重要发现）。** 通过 6 个架构/容量差异
极大的模型（2.4M U-Net ~ 39.6M DeepLabV3+）的逐像素一致性分析发现：模型间
一致率（0.759）显著高于模型与人工标签的一致率（0.650）；多数模型共识与人工
标签仅 0.682 一致；且**容量-性能相关性发生反转**（人工标签下 r=+0.311，
共识参考下 r=−0.444）。这表明当前标注精度已构成模型能力评价的上限约束。

**方法学工作：** 完成数据泄漏核查（MD5 去重 + 三集互斥校验 + test_clean 隔离）、
类别体系核对（依据官方文档确认「背景≠未分类」，修复 no-data 标签处理 bug）、
以及收敛协议设计（Kappa 早停 + EMA + OneCycle）。

**关键词：** 高分辨率遥感；土地覆盖分类；语义分割；Transformer；自注意力；
LoveDA；数据量效应；消融实验

---

## 0. 项目当前状态

| 项 | 状态 |
|---|---|
| 数据准备 | ✅ 官方全量 2,522 张下载完成, 筛选+去重+重划分完成 (newsplit2) |
| 数据筛选 | ✅ no-data ≤10% 筛选, 剔除 312 张低质量图 |
| 泄漏校验 | ✅ train/val/test 两两无交集; test_clean 与旧训练集零重叠 |
| 主模型 | ✅ full_all (2,257张) 训练完成; v2 (筛选数据) 待重训 |
| 对比实验 | 🔄 7 个基线在新数据上待重跑 |
| 消融实验 | 🔄 8 个变体在新数据上待重跑 |
| 关键修复 | ✅ no-data 标签处理 bug 已定位并修复 |

---

## 1. 研究背景与目标

高分辨率遥感土地覆盖分类是遥感智能解译的核心任务。本项目基于自研
**MSSACT-Net**（多尺度自注意力 ConvTransformer），在公开基准 LoveDA 上
完成系统性实验：数据量效应、后训练、推理增强、对比实验、消融实验。

### 1.1 为什么改用公开数据集

早期研究采用自选研究区（国产高分卫星影像）配合 CLCD 公开标签。该方法存在
两个根本问题：

1. **标签与影像不匹配**：CLCD 为 30m 分辨率产品，与 2m 高分影像存在尺度鸿沟，
   标签错位导致监督信号不可靠
2. **类别分布极端**：该区域森林占比超过 80%，模型倾向单类预测即可获得高 OA，
   结论不具备代表性与可比性

改用 LoveDA（0.3m 航空正射 + 官方人工逐像素标注 + 社区公认基准）后，
类别分布均衡（最大类 45.6%）、标签精度可靠、结果可与文献直接对比。

---

## 2. 数据集与划分

### 2.1 官方类别体系（已核实官方 README）

> Category labels: **background – 1, building – 2, road – 3, water – 4,
> barren – 5, forest – 6, agriculture – 7**. And the **no-data regions were
> assigned 0 which should be ignored**.

| 值 | 类别 | 说明 |
|---|---|---|
| **0** | **no-data（未标注）** | **官方要求忽略，不参与训练与评估** |
| 1 | 背景 | 官方正式类别（不属其他6类的区域），最大类 |
| 2 | 建筑 | — |
| 3 | 道路 | — |
| 4 | 水域 | — |
| 5 | 裸地 | — |
| 6 | 森林 | — |
| 7 | 农田 | — |

**重要澄清**：「背景」不是「未分类」；真正的未分类是值 0（no-data）。

### 2.2 数据筛选与重划分（newsplit2）

| 步骤 | 结果 |
|---|---|
| 官方训练区原图 | 2,522 张 |
| no-data 占比分析 | 75% 的图无 no-data；312 张有 10–53% 的 no-data |
| **筛选（no-data ≤10%）** | 剔除 312 张，保留 2,210 张 |
| MD5 去重 | 去除与历史镜像的重复 |
| 8:1:1 划分（seed=42） | **train 1,768 / val 221 / test 221** |
| **test_clean** | **141 张**（额外排除旧模型训练集，用于新旧公平对比） |

### 2.3 泄漏校验（已通过）

- train ∩ val = ∅，train ∩ test = ∅，val ∩ test = ∅
- test_clean ⊆ test
- test_clean ∩ 旧模型训练集 = ∅
- 筛选后 train 的 no-data 均值 **0.13%**、最大 8.27%（阈值内）

---

## 3. 评价体系

### 3.1 指标选择：Kappa 而非 OA

κ = (pₒ − pₑ)/(1 − pₑ)，校正随机一致性。LoveDA 最大类（背景）占 45.6%，
**全预测背景的 OA=45.6% 但 Kappa=0**——OA 在不平衡数据上会被单类支配。
**全部早停与选优均以验证 Kappa 为准**。

### 3.2 数据使用协议

| 集合 | 梯度更新 | 参与选优 | 角色 |
|---|---|---|---|
| train | ✅ 是 | — | 学习 |
| **val** | **❌ 否**（`torch.no_grad()`） | ✅ 是（早停/选优） | 监控 |
| **test / test_clean** | ❌ 否 | ❌ 否 | **最终无偏评估** |

### 3.3 no-data 处理（含修复记录）

**官方协议**：no-data（值 0）必须忽略。

**已修复的 bug**：早期 `REMAP` 实现将值 0 映射为 0（背景类），导致约 3% 的
no-data 像素被错误当作背景参与训练与评估。修复后（0 → 255 ignore）同一模型
指标变化：

| | OA | Kappa |
|---|---|---|
| 修复前（0→背景） | 0.6862 | 0.6114 |
| **修复后（0→ignore）** | **0.6770** | **0.6040** |
| 差异 | −0.0092 | **−0.0074** |

即此前指标**虚高约 0.007–0.009**。修复已生效，后续实验全部使用正确协议。

---

## 4. 训练协议

统一协议（所有实验一致）：

- 优化器：AdamW(lr=2e-4, weight_decay=0.02)
- 调度：OneCycle（6% warmup + 余弦退火，60 轮）
- 权重平滑：EMA(decay=0.999)
- 损失：类别加权交叉熵（频率逆平方根）
- 采样：训练每图随机裁剪 256×256；验证每图 4 角固定 patch
- 早停：验证 Kappa patience=20

---

## 5. 已完成实验结果

### 5.1 数据量效应（核心结果）

| 阶段 | 训练数据 | 干净测试 Kappa | OA | mF1 | mIoU |
|---|---|---|---|---|---|
| 基线 | 957 张 | 0.3921 | 0.5255 | 0.4532 | 0.3061 |
| +数据量 | 2,257 张 | 0.6114* | 0.6862* | 0.6946* | 0.5420* |
| +后训练 | 继续训练 lr=5e-5 | 0.6233* | 0.6968* | 0.7048* | 0.5536* |
| **+TTA** | 8× 几何变换平均 | **0.6278*** | **0.7009*** | **0.7089*** | **0.5583*** |

> *注：标星数值使用修复前的 REMAP（含 no-data 计入背景，约虚高 0.007–0.009）。
> 修复版重训结果 (v2) 待补。

![最终对比](FIG_FINAL)

### 5.2 学习曲线

![学习曲线](FIG_CURVES_NEW)

全量模型（红）第 8 轮起飞、第 35 轮达 0.61；旧模型（灰）缓慢爬至 0.40 后平台化。

### 5.3 每类 F1 提升

![每类F1](FIG_FINAL_PERCLASS)

| 类别 | 957张 | 最终 | 提升 |
|---|---|---|---|
| 道路 | 0.284 | 0.674 | **+0.390** |
| 水域 | 0.431 | 0.817 | **+0.385** |
| 裸地 | 0.203 | 0.527 | +0.324 |
| 建筑 | 0.437 | 0.692 | +0.255 |
| 农田 | 0.552 | 0.806 | +0.254 |
| 森林 | 0.679 | 0.809 | +0.130 |
| 背景 | 0.587 | 0.637 | +0.051 |

**少数类获益最大**，印证小类样本稀缺是此前精度低的主因。

### 5.4 混淆矩阵与统计诊断

![混淆矩阵](FIG_CM_NORM)

关键错误流向（新模型）：各类召回 0.72–0.84，仅**背景类召回 0.53**——其像素
主要流向建筑（0.18）与裸地（0.10）。原因是背景类定义宽泛（含道路边缘、
建筑阴影、田间小路等异质内容），是模型的主要剩余难点。

![混淆矩阵计数](FIG_CM_COUNT)

### 5.5 参数效率

![效率](FIG_EFF)

MSSACT-Net 以 **6.1M 参数**占据帕累托前沿——DeepLabV3+ 需 39.6M 参数才达到
0.38 Kappa。

### 5.6 测试集预测对比

![测试集对比1](FIG_TEST1)

![测试集对比2](FIG_TEST2)

三条曲线（旧模型 / 全量 / 后训练）对比：旧模型大面积误判水域、建筑丢失；
全量模型清晰还原建筑群、道路网、水域边界。

### 5.7 对比实验（957 数据，历史结果）

| 模型 | 参数量 | Kappa | OA |
|---|---|---|---|
| **MSSACT-Net (light)** | **6.10M** | **0.4065** | **0.5630** |
| DeepLabV3+ (R50) | 39.6M | 0.3806 | 0.5092 |
| FCN-32s (R50) | 23.7M | 0.2837 | 0.4245 |
| PSPNet (R18+PPM) | 13.6M | 0.2513 | 0.3834 |
| SegFormer-Lite | 1.01M | 0.1775 | 0.3550 |
| U-Net (base=32) | 2.44M | 0.0939 | 0.4055 |
| FPN (R50) | 27.2M | 0.2982 | 0.4328 |
| Swin-Unet (lite) | 32.6M | 0.2682 | 0.3878 |

### 5.8 消融实验（957 数据，历史结果）

**A) 60 轮预算**

| 配置 | Kappa | ΔKappa |
|---|---|---|
| Full | 0.4065 | — |
| w/o Adapter | 0.3452 | −0.061 |
| w/o Transformer | 0.3636 | −0.043 |
| w/o FPN | 0.3760 | −0.031 |
| w/o ECSAM | 0.3134 | −0.093 |
| w/o EMR | 0.0004 | 坍缩 |

**B) 120 轮预算（收敛后重评）**

| 配置 | Kappa | ΔKappa vs full120 |
|---|---|---|
| full (120ep) | 0.3862 | — |
| w/o Adapter | **0.4621** | **+0.076** |
| w/o Transformer | 0.4580 | +0.072 |
| w/o FPN | 0.4416 | +0.055 |
| w/o EMR | 0.4055 | +0.019 |
| w/o ECSAM | 0.3587 | −0.028 |

![消融](FIG_ABLATION)

**关键发现**：收敛协议下仅 **ECSAM 正贡献**；**EMR 的「坍缩」系早停误杀**
（120 轮恢复至 0.4055）。

### 5.9 模块移除的架构适配与训练响应

| 变体 | 架构适配 | 训练响应 |
|---|---|---|
| w/o EMR | EMR → 标准 ResBlock | 60轮/pt15 坍缩；120轮/pt20 恢复 |
| w/o ECSAM | 跳过 3 阶段坐标注意力 | 稳定，上限降低 |
| w/o FPN | 解码器只收最深特征 | 收敛更快，长期最优 |
| w/o Transformer | 移除全局自注意力 | 稳定，上限略低 |
| w/o Adapter | 仅移除 Adapter-Scale | 小数据上是负担 |
| Trans-4L/6L | 层数 2→4/6 | 4L=0.3907, 6L=0.4026 |
| Bilinear-Up | 转置卷积 → 双线性 | 60轮坍缩；lr=1e-4 可救援 |

### 5.10 策略探索

| 策略 | Kappa | 说明 |
|---|---|---|
| **SGDR 热重启 120轮** | **0.4183** | 最优 |
| 30.5M 原版 OneCycle-60 | 0.4186 | 原版架构可收敛 |
| 快退火 30轮 | 0.10–0.16 | 不足 |
| DeepLab 快退火 | 0.3331 | 基线亦有上限 |

![策略曲线](FIG_STRATEGY_CURVES)

### 5.11 过拟合诊断

训练集 vs 验证集 logits 熵差（ΔH<0 表示过拟合）：

| 模型 | ΔH |
|---|---|
| MSSACT (957张) | −0.241 |
| DeepLabV3+ | −0.194 |
| Swin-Unet | −0.144 |
| U-Net | −0.0003（欠拟合） |
| SegFormer-Lite | +0.032（校准良好） |

数据量提升后泛化差距显著缩小（测试集与验证集 Kappa 接近）。

### 5.12 标签质量上限的实证检验

**动机**：当多个架构与容量差异极大的模型表现接近时，需判断性能上限由模型能力
还是标签质量决定。

**方法**：在 test_clean（141 张）上用 6 个模型（U-Net 2.4M / MSSACT 6.1M /
FCN 23.7M / FPN 27.2M / Swin-Unet 32.6M / DeepLabV3+ 39.6M）逐像素推理，比较
「模型间一致率」「模型-标签一致率」「多模型多数投票共识」，并按距类边界的距离分层。

![标签质量上限证据](FIG_LABEL_LIMIT)

| # | 证据 | 数值 | 含义 |
|---|---|---|---|
| 1 | 模型间一致率 vs 模型-标签一致率 | **0.759 vs 0.650** | 模型互相认同远高于认同人工标签 |
| 2 | 共识标签 vs 人工标签 | **0.682** | 约 31.8% 像素上多数模型与人工标注不符 |
| 3 | **容量-性能相关性反转** | 人工 r=**+0.311** → 共识 r=**−0.444** | 大模型更贴合人工标签模式而非更接近共识 |
| 4 | 边界像素占比 | 边界 **2.4%** / 内部(>8px) **78.4%** | 标签并不碎片化 (原 93.0% 系跨图差分误判, 已修正) |
| 5 | 准确率随距边界距离变化 | 边界 0.45 → 内部 0.70 (单调上升) | 边界是标签最不确定处 (原表述方向错误, 已修正) |

**结论**：6 个模型的能力差异（Kappa 0.61–0.69，跨度 0.08）已落在标签噪声带来的
不确定性范围内；容量-性能相关性随参考标准反转，说明**当前标注精度已成为模型能力
评价的上限约束**。进一步区分架构优劣需要更高精度的人工标注数据（多标注者一致性
标注、更严格的边界规范）。

**理论支撑**：
- 标签噪声下的贝叶斯上限：ε_min ≈ ε_Bayes + η·L/(L−1)（η 为噪声率，L 为类别数）
- 学习曲线幂律：ε(n) = a·n^(−α) + ε_∞，其中 ε_∞ 由标签噪声决定（Hestness et al. 2017, arXiv:1712.00409）；模型改进降低常数项但不改变指数 α
- 谱结构解释：大模型有更大"谱覆盖范围"，但在噪声主导的谱尾无法获得有效信号（Bahri et al. PNAS 2024）

**文献佐证**：遥感分割领域已有工作直接论证此瓶颈——NSegment+（arXiv:2508.10383）
指出真实数据集存在"隐式标签噪声"（模糊边界、标注者差异）并限制性能，仅在**标签端**
做弹性形变即可在 **LoveDA 上获得 +2.38 mIoU**；另有数据中心基准工作专门用于标签噪声
估计与样本排名，指出像素级标注劳动强度大、难以逐像素准确标注。这与本报告发现一致：
性能瓶颈已在标注端而非模型端。

---

---

## 6. 关键结论

1. **数据量是精度主导因素**：2.4× 数据带来 +0.219 Kappa，远超架构/策略调整
2. **少数类可修复**：道路/水域/裸地/建筑 F1 提升 0.25–0.39
3. **MSSACT-Net 参数效率优**：6.1M 参数达到/超过 13–40M 基线
4. **训练策略决定稳定性**：多组「坍缩」实为协议敏感，非架构缺陷
5. **后训练与 TTA 提供边际增益**（合计 +0.017）

---

## 7. 疏忽与教训

### 7.1 研究区选择的疏忽

早期直接采用自选研究区+ 公开标签产品（CLCD），未事先核对
**标签分辨率与影像分辨率的匹配性**（30m vs 2m），也未检查**类别分布是否
具代表性**（森林 >80%）。结果：标签错位 + 单类支配，模型指标看似可用
（OA 76.8%）但实际不具备科学意义，结论无法与文献对比。

**教训**：实验设计阶段必须先完成「数据可用性三查」——①标签精度与影像
分辨率匹配；②类别分布是否均衡；③是否有公认基准与可对比结果。

### 7.2 类别体系核对的疏忽

在改用 LoveDA 后，未第一时间逐项核对官方类别定义，将「背景（值1）」与
「未分类（值0）」混淆，导致 `REMAP` 将 no-data 错误并入背景类，使全部实验
指标虚高约 0.007–0.009。

**教训**：使用任何公开数据集前，必须以**官方文档原文**为准核对：类别编号、
每类语义、无效值定义与评估协议。此项疏漏由提问发现并修复，后续所有实验
均按官方协议执行。

### 7.3 已建立的质量控制

| 控制项 | 措施 |
|---|---|
| 数据泄漏 | MD5 去重 + 三集两两校验 + test_clean 隔离旧训练集 |
| 指标可信 | 全部以 Kappa 选优；训练/验证/测试协议明确；train 不用于报告 |
| 标签正确 | 以官方 README 原文为准；no-data 显式 ignore |
| 结果可复现 | 全部实验保留 history.json + 权重 + 固定 seed |
| 架构一致性 | checkpoint 架构自动核验（verify_experiments.py） |

---

## 8. 接下来的项目安排

### 阶段一：筛选数据上的完整重跑（进行中，约 50 小时）

| 序号 | 任务 | 说明 |
|---|---|---|
| 1 | **主模型重训** | full_all_v2（筛选数据 1,768 张, 60 轮） |
| 2 | **后训练** | post_all_v2（从 v2 权重继续训练） |
| 3 | **对比实验 ×7** | U-Net / PSPNet / FCN / DeepLabV3+ / SegFormer / FPN / Swin-Unet |
| 4 | **消融实验 ×8** | no_emr / no_ecsam / no_fpn / no_trans / no_adapter / Trans-4L / Trans-6L / Bilinear |
| 5 | **统一评估** | 全部模型在 test / test_clean 上评估（含 TTA） |

### 阶段二：结果汇总与图表更新（约 1 天）

- 生成新版对比表、消融表、混淆矩阵、学习曲线
- 更新项目报告与 README
- 修正历史数字（标注 REMAP 修复前后差异）

### 阶段三：成果整理与学术输出（约 1 周）

- 整理完整实验记录与技术文档（方法、协议、全部结果）
- 总结方法学经验：数据泄漏防控、类别体系核对、收敛协议设计
- 梳理可延伸的学术问题（见阶段四），为后续研究选题做准备

### 阶段四：后续研究展望

1. **标签质量与评价体系**：引入多标注者一致性标注，估计真实标签噪声率；在更高
   精度标注数据上重新评价各架构能力
2. **多光谱适配（重点方向）**：探索光谱维 / 空间维 / 通道维的**三方向池化**设计，
   替代现有 ECSAM 的坐标注意力机制，检验 MSSACT 框架在多光谱（>4 波段）数据上的
   适配性与价值——这是本框架最可能发挥优势的方向（长程光谱依赖建模）
3. **跨域验证**：GF-1 2.4m 影像 + 同域微调，检验分辨率-域差异下的迁移能力
4. **数据-容量匹配**：基于已建立的数据阶梯（250/500/1000/1768），寻找该任务的最优模型容量
5. **少数类与部署**：重采样/损失调优；推理加速
---

## 9. 个人工作与能力体现

本项目从数据获取到结果验证全流程独立完成，主要工作与能力体现如下：

### 9.1 数据工程

- **数据获取与整合**：从 HuggingFace 镜像定位并获取 LoveDA 官方全量训练集
  （2,522 张, 4GB），完成解压、格式核对与目录组织
- **数据质量控制**：编写筛选脚本按 no-data 占比过滤低质量图（剔除 312 张
  10–53% 无标注的样本）；MD5 去重避免重复样本；8:1:1 重划分并做泄漏校验
  （三集两两互斥 + test_clean 隔离历史训练集）

### 9.2 模型训练与工程

- **训练管线**：PyTorch + AMP 混合精度 + EMA 权重平滑 + OneCycle 调度 +
  类别加权损失；单卡 8GB 显存下完成 6.1M ~ 39.6M 参数模型训练
- **自动化队列**：实现断点续跑的串行实验队列（42+ 组实验），含任务状态
  判定、失败重试、显存不足自动降 batch、历史完整性校验
- **实时监控**：开发动态刷新监控面板（阶段/任务/epoch/指标/剩余时间估算）

### 9.3 实验设计与分析

- **公平性设计**：统一训练协议（同数据划分、同步数、同调度）、以 Kappa
  为选优标准（避免 OA 在不平衡数据上被单类支配）、val 仅用于选优不参与训练
- **消融与对比**：完成 7 个基线模型对比、8 个消融变体、11 组训练策略探索，
  并按收敛协议重评模块贡献（发现单预算消融的误导性）
- **统计诊断**：混淆矩阵、每类精度/召回/F1、参数效率、logits 熵过拟合诊断

### 9.4 问题定位与修复

- **标签协议 bug**：通过核对官方文档发现「背景」与「未分类」混淆，定位
  REMAP 实现缺陷，量化影响（指标虚高 0.007–0.009）并修复
- **数据泄漏**：发现历史数据与新测试集 90 张重叠，构建干净测试子集保证
  对比公平
- **训练稳定性**：定位多组「坍缩」现象为调度-模型不匹配而非架构缺陷，
  通过策略调整（SGDR / 低 LR 长退火）使全部变体可收敛

### 9.5 工具与产出

- 自动化脚本：`resplit_filtered.py`（筛选+划分+校验）、`run_queue_new.py`
  （实验队列）、`eval_complete.py`（统一评估）、`make_figures.py`（图表）、
  `make_project_report.py`（Markdown→HTML→PDF 报告管线）
- 全部代码与方法学文档已开源至 GitHub（https://github.com/eugenewang5425/mssact-loveda）

---

## 附录：复现命令

```bash
python resplit_filtered.py       # no-data筛选 + 去重 + 8:1:1 重划分
python verify_split2.py          # 泄漏校验
python run_queue_new.py          # 主模型重训 + 对比(7) + 消融(8)
python eval_complete.py          # val/test × TTA 评估
python make_figures.py           # 图表生成
python make_report_pdf.py        # 报告生成
```

**代码仓库**: https://github.com/eugenewang5425/mssact-loveda
"""

for k, v in [("FIG_FINAL","fig_final_comparison.png"),("FIG_CURVES_NEW","fig_new_learning_curves.png"),
             ("FIG_FINAL_PERCLASS","fig_final_perclass.png"),("FIG_CM_NORM","fig_confusion_norm.png"),
             ("FIG_CM_COUNT","fig_confusion_count.png"),("FIG_EFF","fig_efficiency.png"),
             ("FIG_TEST1","fig_test_showcase_00000.png"),("FIG_TEST2","fig_test_showcase_00006.png"),
             ("FIG_ABLATION","fig_ablation.png"),("FIG_STRATEGY_CURVES","fig_strategy_curves.png"),
             ("FIG_LABEL_LIMIT","fig_label_limit.png"),("FIG_PRF","fig_prf_perclass.png"),
             ("FIG_LR","fig_lr_schedule.png"),("FIG_CURVES_NEWDATA","fig_curves_newdata.png")]:
    if has(v):
        MD = MD.replace(k, img_b64(f"{FIG}/{v}"))
    else:
        MD = MD.replace(k, "")
        print(f"  [warn] missing figure: {v}")

html_body = markdown.markdown(MD, extensions=["tables", "fenced_code"])
html = f"""<!DOCTYPE html>
<html><head><meta charset="utf-8">
<style>
body {{ font-family: "Microsoft YaHei", "SimSun", sans-serif; margin: 40px; line-height: 1.75; color: #222; }}
h1 {{ color: #b30000; border-bottom: 3px solid #b30000; padding-bottom: 8px; }}
h2 {{ color: #333; border-bottom: 1px solid #ccc; padding-bottom: 5px; margin-top: 34px; }}
h3 {{ color: #555; margin-top: 22px; }}
table {{ border-collapse: collapse; width: 100%; margin: 14px 0; font-size: 13px; }}
th, td {{ border: 1px solid #bbb; padding: 6px 10px; text-align: left; }}
th {{ background: #f0f0f0; }}
img {{ max-width: 100%; margin: 12px 0; border: 1px solid #ddd; }}
code {{ background: #f5f5f5; padding: 2px 5px; border-radius: 3px; font-size: 13px; }}
pre {{ background: #f5f5f5; padding: 12px; border-radius: 5px; overflow-x: auto; }}
blockquote {{ border-left: 4px solid #b30000; padding-left: 12px; color: #555; background:#fafafa; }}
</style></head><body>
{html_body}
</body></html>"""

open(f"{BASE}/项目报告.html", "w", encoding="utf-8").write(html)
print("HTML written:", f"{BASE}/项目报告.html")
